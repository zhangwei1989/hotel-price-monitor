# 2026-03-03

- 项目命名：PriceWatcher（主项目），PriceWatcher Dashboard（Web 应用）。
- 已实现：携程酒店价格监控任务存储在 `ctrip-monitor-state.json`；默认监控频率改为每 8 小时；监控默认在入住日期隔天（autoStopDate）停止；阈值更新为低于 ¥672 才通知。
- 浏览器/环境：本机终端已通过 nvm 升级到 Node v22.22.0（用于运行 Vite 7）。EasyClaw 的 exec 环境默认 Node v16，无法直接 `npm run dev` 启动 Vite 7，需要用本机终端或用 nvm 的 node 直接运行 `node node_modules/vite/bin/vite.js`。
- PriceWatcher API：采用单一超级管理员账号登录（admin），JWT 鉴权；API 服务在端口可能冲突时改跑 3002。
- PriceWatcher Dashboard：已实现登录页/路由守卫/任务列表与任务详情/历史；用户确认已正常登录并能查看列表与详情。
- 用户决定：需要“暂停/恢复任务”功能（选项 A）。已新增接口 `POST /api/task-actions/:id/pause|resume` 与列表筛选 enabled；Dashboard 列表与详情页均加入暂停/恢复按钮。
